<!doctype html>
<html lang="<%= site.locale %>">
  <head>
    <%= render "head", metadata: site.metadata, title: data.title %>
  </head>
  <body data-controller="view-more"
        class="<%= data.layout %> <%= data.page_class %> flex flex-col min-h-svh text-neutral">
    <%= yield %>
  </body>
</html>
